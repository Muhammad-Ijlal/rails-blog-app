<h1> Showing article details </h1>

<p><strong>Title: <%= @article.title %> </strong></p>
<p><strong>Description: <%= @article.description %> </strong></p>

<table>
  <tbody>
      <tr> 
        <td><%= link_to 'Listing', articles_path %></td>
        <td><%= link_to 'Edit', edit_article_path(@article) %></td>
        <td><%= link_to 'Delete', article_path(@article), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></td>
      </tr>
  </tbody>
</table> 